[build]
  base = "."
  command = "cd gatsby && npm install && npm run build"
  publish = "gatsby/public"

[build.environment]
  NODE_VERSION = "18"

[functions]
  directory = "netlify/functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
